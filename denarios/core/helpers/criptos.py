def criptos(par=None):

    criptos = [
    'ETH' + par if par else 'ETH',
    'ETC' + par if par else 'ETC',
    'DOGE' + par if par else 'DOGE',
    'XRP' + par if par else 'XRP',
    'ADA' + par if par else 'ADA',
    'BNB' + par if par else 'BNB',
    'USDT' + par if par else 'USDT',
    'BCH' + par if par else 'BCH',
    'EOS' + par if par else 'EOS',
    'DOT' + par if par else 'DOT',
    'AXS' + par if par else 'AXS',
    'PAX' + par if par else 'PAX',
    'SOL' + par if par else 'SOL',
    ]

    return criptos
